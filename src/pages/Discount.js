import React from 'react';
import { Container } from '@mui/material';

function Discount() {
    return (
        <Container maxWidth="lg">
            This is the discount page
        </Container>
    )
};

export default Discount;
